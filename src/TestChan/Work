1、信道是协程之间通信的管道，信道都关联有一个类型，信道只能运输该类型的数据，而运输其他类型是非法的。同样是使用make来定义通道
2、信道的发送与接收默认是阻塞的，当数据发送到信道时，程序控制会在发送数据的语句处阻塞，直到有其它协程读取到数据才会解除阻塞。读取信道的数据也是这样
3、缓冲信道在缓冲已满时才会阻塞向缓冲信道发送数据
4、缓冲信道同样分有长度和容量
5、WaitGroup是一个结构体类型。 *计数器应该使用指针传值，不然得到的是值拷贝