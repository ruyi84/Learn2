1、接口的声明与实现
    Demo1中，流程;
        定义一个包含FindeVowels方法的接口VowelsFinder，定义一个String类型的MyString类型，MyString类型实现了V接口的Find方法。
        main中，定义了My类型的name变量，定义了V接口类型的v变量，因为name变量实现了vow接口，所以可以赋值给v。这类似于java中的父类引用指向子类对象。


2、接口的实际用途
    Demo2，间接实现多态
    创建了一个包含Cal方法的salar接口，创建了两个结构体（P,C）分别实现了Salar借口，又创建了一个接受切片类型参数的方法，其中可以调用上面结构体实例化的方法。近似于多态

    3、空接口
    定义一个函数时，可以在参数列表中加入一个空接口，这样他就可以接受任何类型。
    空接口中没有方法，所以所有类型都实现了空接口

    4、类型断言
    通过类型断言，可以用于获得接口的底层值，也就是通过接口包装之前的基础类型的值及类型

    5、类型选择
     类型选择类似于类型断言，将（）里的具体类型T换成了Type， 但是要结合在switch
    6、接口的嵌套