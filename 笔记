1、go里的数组之间的复制是值传递
2、切片的赋值是引用传递，所以修改时是修改的是地址内的值
3、数组的地址一般是数组内第一个值得地址
4、切片的初始化方法，专业术语叫做切片字面量。
5、切片包括两个属性，长度和容量
6、讲一个数组切割赋值给一个切片时，则赋值后的切片的长度为赋值内容的长度，切片的容量为数组的长度
7、go的结构体可以存储方法
8、go的结构体可以匿名字段，默认将类型作为字段名，但是一个类型只允许有一个
9、go借助接口的实现可以完成java中的多态，不同的结构体通过实现供一个接口，然后通过相同的结构体.方法的方式，来完成多态
